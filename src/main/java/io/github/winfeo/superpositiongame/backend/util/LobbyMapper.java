package io.github.winfeo.superpositiongame.backend.util;

import io.github.winfeo.superpositiongame.backend.dto.LobbyResponseDto;
import io.github.winfeo.superpositiongame.backend.dto.UserDto;
import io.github.winfeo.superpositiongame.backend.entity.User;

import java.util.Set;
import java.util.stream.Collectors;

public class LobbyMapper {
//    public LobbyResponseDto toDto(Set<User> users) {
//        return new LobbyResponseDto(
//                users.stream()
//                        .map(user -> new UserDto(user.getId()))
//                        .collect(Collectors.toSet())
//        );
//    }
}
